import { Meta } from '@storybook/blocks'
import * as IconStories from './Icon.stories'
import { IconGallery } from './IconGallery'

<Meta of={IconStories} name="Gallery" />

# Icons

Material Symbols (**Outlined**, weight 200) — the actual Google symbols, rendered via
`<Icon name="…" />`. Search the full library below and click any icon to see its name,
sizes, and usage snippet. Icons load on demand, so browsing the whole set stays fast.

<IconGallery />
